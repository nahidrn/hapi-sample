'use strict';

/*
//TODO: create a transport config for this

if (LOGGER_LOGGLY_TOKEN && LOGGER_LOGGLY_SUBDOMAIN) {
  require('winston-loggly-bulk');
  transports.push(new (winston.transports.Loggly)({
    token: LOGGER_LOGGLY_TOKEN,
    subdomain: LOGGER_LOGGLY_SUBDOMAIN,
    tags: ['api', NODE_ENV],
    json: false
  }));
}
*/
